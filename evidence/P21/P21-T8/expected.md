## P21-T8 Evidence

- `/trash` route must render via unified `FilesPage` path.
- `FilesPage` must include trash route handling and i18n tokens (`nav.trash`, `msg.emptyTrash`).
- CI evidence target: `packages/ui` test for `FilesPage.spec.tsx` must pass.
