# P24-T0 Expected Evidence

- SSOT source: `design/stitch/ko-kr_final/inventory/route_reference_map.md`
- Acceptance: every IA route must be mapped to `reference(code.html)` and `reference(screen.png)`.
- For routes without direct reference, `derived_link` must be non-empty and point to a derived spec.
- Result: PASS only when all IA routes exist with non-placeholder mapping rows.
