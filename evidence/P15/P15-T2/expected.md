# P15-T2 Login UI + POST /auth/login

## Goal
- Provide a login screen with username/password inputs.
- Connect the form submission to POST /auth/login and persist returned tokens.

## Evidence
- `pnpm -C packages/ui test`
- `pnpm -C packages/ui build`
