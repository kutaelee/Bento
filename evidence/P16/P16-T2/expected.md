# P16-T2 Breadcrumb

## Goal
- Render breadcrumb for `/files` and `/files/:nodeId` using `GET /nodes/{node_id}/breadcrumb`.
- Breadcrumb items navigate to parent folders when clicked.

## Evidence
- `pnpm -C packages/ui test`
