id: P2-T1-AUTH-LOGIN-001
name: auth login success
steps:
  - name: seed initial admin
    request:
      method: POST
      path: /setup/admin
      json:
        username: admin
        password: CorrectHorseBatteryStaple
        display_name: Admin
        locale: en-US
    expect:
      status: 201
  - name: login
    request:
      method: POST
      path: /auth/login
      json:
        username: admin
        password: CorrectHorseBatteryStaple
    expect:
      status: 200
      json:
        has:
          - user
          - tokens
